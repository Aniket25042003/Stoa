# API process (no browser). Worker needs Chromium installed at image build — see docs/runbook.md + nixpacks.toml.
web: uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}
worker: celery -A app.celery_app worker -l info
